Install the environment builder inside the folder where the .ipynb is saved
```
sudo apt update && sudo apt install python3-venv python3-full -y
```

Create your study environment
```
python3 -m venv .venv
```

Activate it
```
source .venv/bin/activate
```

